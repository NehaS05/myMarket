# Shared

Shared TypeScript contracts can be moved here when the frontend and backend are packaged as a workspace. The MVP currently duplicates a small set of DTOs to keep deployments simple for Vercel and Render Free.
